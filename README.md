# Caster API

#### Backend-сервис на Node.js / Express / PostgreSQL. Основной упор сделан на надежность кода (автотесты) и автоматизацию окружения

### Стек:

* **Язык**: TypeScript (Node.js)
* **База данных**: PostgreSQL + Prisma ORM
* **Развертывание**: Docker Compose
* **Документация**: Swagger

### Особенности:

* **База данных**: Все запросы к базе реализованы через **Prisma**. Настроена автоматическая инициализация данных (seed)
* **Авторизация**: JWT. Для инвалидации токенов используется **Redis**. Сессии хранятся в БД
* **Тестирование**: Написаны интергационные автотесты (**Vitest/Supertest**) для проверки всех эндпоинтов
* **Docker**: Все зависимости (app, db, redis) изолированы и связаны через **Docker Compose**. Настроены проверки готовности (healthchecks)
* **Валидация окружения**: Реализована проверка обязательных переменных `env` при запуске. При отсутствии переменных выдаст ошибку

### Развертывание и запуск (docker-compose):

1. Переименовать файлы env (убрать .example) и указать значения переменных

2. Собрать и запустить приложение: `docker-compose up -d --build`

### Развертывание и запуск (localhost):

1. Переименовать файлы env (убрать .example) и указать значения переменных

2. Убедиться в наличии установленного Postgres. Или запустить в docker: `docker-compose up -d db`

3. Убедиться в наличии установленного Redis. Или запустить в docker: `docker-compose up -d redis`

4. Установить зависимости: `npm install`

5. Сгенерировать клиент Prisma: `npx prisma generate`

6. Запустить приложение: `npm run dev`

### Тестирование:

1. Запустить приложение

2. Убедиться, что в env.test указана тестовая БД

3. Подготовить базу для тестов (для работы с env.test): `npm run test:init`

4. Запустить тесты: `npm run test`

### Доступные ресурсы:

* **Приложение**: `http://localhost:3000`
* **Swagger**: `http://localhost:3000/api-docs`
* **Adminer**: `http://localhost:8080`